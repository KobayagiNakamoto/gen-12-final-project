//import { Form, Container, Col, Row, Button } from 'react-bootstrap';

const Home = (props) => {
    return (
        <div className='home'>
            <p>Home page {props.title}</p>
        </div>
    )
}

export default Home;